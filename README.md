# phpResticAdmin
Web UI and API for restic backup management.

## Quick Start

```bash
cd phpresticadmin
composer install
docker compose -f docker/docker-compose.yml up --build
```

Open http://localhost:8080
